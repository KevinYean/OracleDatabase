Read Me File:

This project is divided into three different portions.
hw3.java has the GUI representation of the Database
populate.java takes input files as command line parameters and populate them into your database.
createdb.sql/dropdb.sql are sql files to create tables and drop them.